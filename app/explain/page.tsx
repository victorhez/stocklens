'use client';
import { useState } from 'react'; import { Bot, Loader2, Sparkles } from 'lucide-react'; import { AppShell } from '@/components/AppShell'; import { Card, Pill } from '@/components/Card'; import { demoPositions } from '@/lib/demo';
export default function Explain(){const [q,setQ]=useState('Why does my Apple token balance stay the same?');const [answer,setAnswer]=useState('');const [loading,setLoading]=useState(false);async function ask(){setLoading(true);const r=await fetch('/api/explain',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({question:q,position:demoPositions[0]})});const j=await r.json();setAnswer(j.answer);setLoading(false)}return <AppShell><div className="mx-auto max-w-4xl px-5 py-8"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-indigo-500/15 text-indigo-200"><Sparkles size={20}/></div><div><Pill>Grounded AI</Pill><h1 className="mt-2 text-3xl font-semibold">Explain my position</h1></div></div><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Ask about the observed data. StockLens only uses the supplied facts and clearly separates calculations from simulations.</p><Card className="mt-6 p-5"><label className="text-xs text-slate-500">Ask StockLens Explain</label><textarea value={q} onChange={e=>setQ(e.target.value)} rows={4} className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-slate-950 p-4 text-sm outline-none focus:border-indigo-400/50"/><button onClick={ask} disabled={loading} className="mt-3 btn-primary">{loading?<Loader2 className="animate-spin" size={15}/>:<Bot size={15}/>}Explain with facts</button></Card>{answer&&<Card className="mt-5 p-6"><div className="flex items-center gap-2 text-indigo-200"><Bot size={17}/>StockLens Explain</div><div className="mt-5 whitespace-pre-wrap text-sm leading-7 text-slate-300">{answer}</div><div className="mt-6 border-t border-white/10 pt-4 text-xs text-slate-500">Data timestamp: {new Date(demoPositions[0].timestamp).toLocaleString()}. This is informational only, not investment advice.</div></Card>}<div className="mt-5 grid gap-3 sm:grid-cols-3">{['What do I hold?','Why did my multiplier matter?','How is my value calculated?'].map(x=><button key={x} onClick={()=>setQ(x)} className="rounded-xl border border-white/10 bg-white/[.03] p-4 text-left text-xs text-slate-300 hover:bg-white/[.06]">{x}</button>)}</div></div></AppShell>}
